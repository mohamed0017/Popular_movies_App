<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory android:title="@string/sort">
        <ListPreference
            android:defaultValue="3"
            android:dialogTitle="@string/sort_title"
            android:entries="@array/pref_sort_entries"
            android:entryValues="@array/pref_sort_values"
            android:key="@string/key_sort"
            android:summary="@string/summary_sort"
            android:title="@string/sort_title" />
    </PreferenceCategory>
</PreferenceScreen>
